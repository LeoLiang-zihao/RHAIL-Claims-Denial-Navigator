# RHAIL Claims Denial Navigator 系统架构说明

## 📋 概述

这个项目**不是**传统的 LLM → MCP 架构。它是一个基于 **Microsoft Power Platform** 的医疗理赔处理系统，使用 **RAG（检索增强生成）** 模式，通过 **Azure OpenAI** 和 **Azure AI Search** 来实现智能理赔分析和推荐。

## 🏗️ 系统架构流程图

```
用户上传文件 (SharePoint)
    ↓
Power Automate 触发器 (监听文件创建)
    ↓
主流程 (DenialNavigator_MainProcessFlow)
    ├─→ 解析文件头 (ParseHeader835)
    │   └─→ HTTP POST → Azure OpenAI
    │       └─→ data_sources: Azure AI Search (parse索引)
    │           └─→ 返回解析后的JSON
    │
    ├─→ 解析835理赔 (Parse835Claims)
    │   └─→ HTTP POST → Azure OpenAI
    │       └─→ data_sources: Azure AI Search (parse索引)
    │           └─→ 返回结构化理赔数据
    │
    ├─→ 解析837理赔 (Parse837Claims)
    │   └─→ HTTP POST → Azure OpenAI
    │       └─→ data_sources: Azure AI Search (parse索引)
    │           └─→ 返回结构化理赔数据
    │
    └─→ 获取推荐 (GetRec)
        └─→ HTTP POST → Azure OpenAI
            └─→ data_sources: Azure AI Search (recs索引)
                └─→ 返回理赔拒绝处理建议
    ↓
存储到 Dataverse (Power Platform数据库)
    ↓
Power Apps 显示结果
```

## 🔑 核心组件说明

### 1. **Power Automate (工作流引擎)**
   - **作用**: 这是整个系统的"大脑"，负责协调所有流程
   - **不是MCP**: Power Automate 是 Microsoft 的低代码自动化平台，不是 MCP 服务器
   - **文件位置**: `solution/Workflows/*.json`

### 2. **Azure OpenAI (LLM模型)**
   - **作用**: 执行实际的AI推理任务
   - **两个主要用途**:
     - **解析任务**: 将835/837文本文件解析成结构化JSON
     - **推荐任务**: 根据CARC/RARC代码生成处理建议
   - **模型**: GPT-4o (在 `azureresources/main.bicep` 中配置)

### 3. **Azure AI Search (RAG数据源)**
   - **作用**: 作为**检索增强生成**的数据源，不是MCP
   - **两个索引**:
     - **parse索引** (`parse-autocreate`): 存储解析指南和示例
     - **recs索引** (`filerecs-autocreate`): 存储理赔处理指南、政策文档、用户反馈等
   - **工作原理**: 
     - 当调用Azure OpenAI时，通过 `data_sources` 参数传入Azure Search配置
     - Azure OpenAI会自动从Search索引中检索相关文档
     - 将检索到的文档作为上下文，增强LLM的生成能力

### 4. **Azure Blob Storage (文档存储)**
   - **作用**: 存储用于RAG的原始文档
   - **两个容器**:
     - `parse`: 存储解析相关的文档
     - `recs`: 存储推荐相关的文档（理赔指南、政策文档等）

### 5. **Dataverse (数据存储)**
   - **作用**: Power Platform的数据库，存储解析后的理赔数据和推荐结果
   - **表结构**: 在 `solution/Entities/` 中定义

## 🔄 详细工作流程

### 流程1: 文件解析 (Parsing)

**触发条件**: 用户上传835或837文件到SharePoint

**执行步骤**:

1. **主流程触发** (`DenialNavigator_MainProcessFlow`)
   ```json
   "triggers": {
     "When_a_file_is_created_(properties_only)": {
       "type": "OpenApiConnection",
       // 监听SharePoint文件创建事件
     }
   }
   ```

2. **调用子流程解析** (`DenialNavigator_Child_Parse835Claims`)
   - 构建HTTP请求到Azure OpenAI
   - **关键配置**:
   ```json
   {
     "messages": [
       {
         "role": "system",
         "content": "You are a Healthcare claim insurance AI assistant..."
       },
       {
         "role": "user",
         "content": "Parse 835 file content..."
       }
     ],
     "data_sources": [
       {
         "type": "azure_search",
         "parameters": {
           "endpoint": "Azure Search URL",
           "index_name": "parse-autocreate",  // 使用parse索引
           "authentication": { "type": "api_key", "key": "..." }
         }
       }
     ]
   }
   ```

3. **Azure OpenAI处理**:
   - 接收用户查询（835文件内容）
   - **自动从Azure Search检索相关文档**（这是RAG的核心）
   - 使用检索到的文档作为上下文
   - 生成结构化的JSON输出

4. **存储结果**: 将解析后的JSON存储到Dataverse

### 流程2: 推荐生成 (Recommendation)

**触发条件**: 用户请求理赔拒绝处理建议

**执行步骤**:

1. **调用推荐流程** (`DenialNavigator_Child_GetRec`)
   - 输入: CARC代码、RARC代码、保险公司名称
   
2. **构建查询**:
   ```json
   {
     "messages": [
       {
         "role": "system",
         "content": "You are an AI assistant that helps people fix medical billing files..."
       },
       {
         "role": "user",
         "content": "Provide recommendations on how to correct the claim for [保险公司]. CARC: [代码]. RARC: [代码]"
       }
     ],
     "data_sources": [
       {
         "type": "azure_search",
         "parameters": {
           "index_name": "filerecs-autocreate",  // 使用recs索引
           "strictness": 5,
           "top_n_documents": 5,  // 检索前5个相关文档
           "fields_mapping": {
             "content_fields": ["content", "metadata_storage_name"],
             "vector_fields": ["contentVector"]  // 向量搜索
           }
         }
       }
     ]
   }
   ```

3. **Azure OpenAI处理**:
   - 从 `filerecs-autocreate` 索引中检索相关文档
   - 这些文档可能包括:
     - 理赔处理指南
     - 保险公司政策文档
     - 用户反馈 (`SubmittedUserFeedback.txt`)
     - CARC/RARC代码定义
   - 基于检索到的文档生成个性化建议

4. **返回结果**: HTML格式的建议和引用来源

## 🆚 与传统LLM架构的区别

### 传统架构（您之前理解的）:
```
用户输入 → MCP服务器 → LLM → 输出
```

### 本项目架构:
```
文件上传 → Power Automate → Azure OpenAI (带RAG) → Dataverse → Power Apps
                ↓
         Azure AI Search (数据源)
```

### 关键区别:

1. **没有MCP服务器**
   - 本项目使用 **Power Automate** 作为工作流引擎
   - 使用 **Azure AI Search** 作为RAG数据源，不是MCP

2. **RAG模式**
   - Azure OpenAI的 `data_sources` 参数直接集成Azure Search
   - LLM在生成时自动检索相关文档
   - 这是**检索增强生成**，不是工具调用

3. **低代码平台**
   - 整个系统构建在Power Platform上
   - 使用JSON配置文件定义工作流
   - 不需要编写传统代码

## 📁 关键文件说明

### Workflow文件 (`solution/Workflows/`)

1. **DenialNavigator_MainProcessFlow.json**
   - 主流程，监听SharePoint文件创建
   - 协调所有子流程

2. **DenialNavigator_Child_Parse835Claims.json**
   - 解析835理赔文件
   - 调用Azure OpenAI解析文本为JSON

3. **DenialNavigator_Child_GetRec.json**
   - 生成理赔拒绝处理建议
   - 使用RAG检索相关文档

4. **DenialNavigator_Child_ParseHeader835.json**
   - 解析835文件头信息

5. **DenialNavigator_Child_Parse837Claims.json**
   - 解析837理赔文件

### Azure资源配置 (`azureresources/`)

1. **main.bicep**
   - 定义Azure资源（OpenAI、Search、Storage）

2. **IndexResource/**
   - 定义Search索引结构
   - `parseIndexProfile.json`: 解析索引配置
   - `recsIndexProfile.json`: 推荐索引配置

### 数据文件 (`data/`)

- **rhail_codedefinitions.csv**: CARC和RARC代码定义

## 🎯 为什么能实现Claim功能？

1. **Power Automate工作流**: 
   - 自动监听文件上传
   - 协调多个处理步骤
   - 处理错误和重试

2. **Azure OpenAI + RAG**:
   - 通过Azure Search检索理赔处理指南
   - 基于实际文档生成建议
   - 支持向量搜索，找到最相关的文档

3. **结构化数据存储**:
   - Dataverse存储解析后的结构化数据
   - 支持查询和分析

4. **Power Apps界面**:
   - 提供用户友好的界面
   - 显示解析结果和推荐建议

## 🔍 数据流示例

### 示例: 生成理赔拒绝建议

1. **用户操作**: 在Power Apps中选择一个被拒绝的理赔

2. **Power Automate触发**: 
   - 调用 `DenialNavigator_Child_GetRec`
   - 传入: CARC="CO-16", RARC="N255", Insurance="North Dakota Medicaid"

3. **HTTP请求到Azure OpenAI**:
   ```http
   POST https://[your-openai-endpoint]/openai/deployments/gpt-4o/chat/completions?api-version=2024-05-01-preview
   Headers:
     api-key: [your-key]
   Body:
   {
     "messages": [...],
     "data_sources": [{
       "type": "azure_search",
       "parameters": {
         "index_name": "filerecs-autocreate"
       }
     }]
   }
   ```

4. **Azure OpenAI内部处理**:
   - 自动查询Azure Search索引
   - 检索与"CO-16"、"N255"、"North Dakota Medicaid"相关的文档
   - 可能检索到的文档:
     - CARC CO-16的定义
     - RARC N255的说明
     - North Dakota Medicaid的理赔处理指南
     - 用户反馈（如果有）

5. **生成响应**:
   - 基于检索到的文档生成HTML格式的建议
   - 包含引用来源（citations）

6. **返回结果**:
   - Power Automate接收响应
   - 存储到Dataverse
   - Power Apps显示给用户

## 💡 总结

这个项目的架构特点:

1. **不是MCP架构**: 使用Power Automate + Azure OpenAI + Azure Search
2. **RAG模式**: Azure Search作为数据源，增强LLM生成
3. **低代码平台**: 基于Power Platform，使用JSON配置
4. **自动化流程**: 从文件上传到结果展示全自动
5. **企业级集成**: 与SharePoint、Dataverse、Power Apps深度集成

这种架构的优势:
- ✅ 无需编写复杂代码
- ✅ 易于维护和扩展
- ✅ 企业级安全和合规
- ✅ 支持大规模文档检索
- ✅ 自动化的端到端流程
