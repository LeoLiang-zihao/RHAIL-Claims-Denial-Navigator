# 工作子流程详解

## 📚 什么是工作子流程？

### 简单理解

**工作子流程（Child Workflow/Subprocess）** 就像编程中的**函数**或**子程序**：

- **主流程** = 主程序（负责整体协调）
- **子流程** = 函数（负责完成特定任务）

### 类比说明

想象您要做一个复杂的项目：

```
🏢 主流程 = 项目经理
   ├─ 📋 子流程1 = 设计师（负责设计）
   ├─ 🔨 子流程2 = 工程师（负责建造）
   └─ ✅ 子流程3 = 质检员（负责检查）
```

主流程（项目经理）负责：
- 协调整个项目
- 决定什么时候调用哪个子流程
- 传递数据给子流程
- 接收子流程的结果

子流程（各个专业人员）负责：
- 完成自己专业领域的任务
- 接收主流程传来的数据
- 处理完成后返回结果给主流程

---

## 🔍 在Power Automate中的子流程

### 基本概念

在Power Automate（Microsoft的低代码自动化平台）中：

1. **主流程（Parent Workflow）**
   - 可以独立运行
   - 可以调用其他流程
   - 通常由事件触发（如文件上传）

2. **子流程（Child Workflow）**
   - 被其他流程调用的流程
   - 可以接收输入参数
   - 可以返回输出结果
   - 可以被多个主流程调用（代码复用）

### 为什么使用子流程？

#### ✅ 优势

1. **代码复用**
   - 一个子流程可以被多个主流程调用
   - 避免重复编写相同逻辑

2. **模块化设计**
   - 每个子流程专注于一个任务
   - 更容易维护和调试

3. **简化主流程**
   - 主流程更清晰易读
   - 不需要把所有逻辑都写在一个流程里

4. **独立测试**
   - 可以单独测试每个子流程
   - 更容易定位问题

---

## 📊 本项目中的子流程

### 主流程和子流程关系

```
DenialNavigator_MainProcessFlow (主流程)
   │
   ├─→ DenialNavigator_Child_ParseHeader835 (子流程1)
   │   └─ 解析835文件头
   │
   ├─→ DenialNavigator_Child_Parse835Claims (子流程2)
   │   └─ 解析835理赔数据
   │
   ├─→ DenialNavigator_Child_Parse837Claims (子流程3)
   │   └─ 解析837理赔数据
   │
   └─→ DenialNavigator_Child_GetRec (子流程4)
       └─ 生成理赔拒绝处理建议
```

---

## 🔧 实际代码示例

### 主流程如何调用子流程

在主流程的JSON配置中，调用子流程的代码：

```json
{
  "Parse_835_Claims": {
    "type": "Workflow",  // 类型：工作流（子流程）
    "inputs": {
      "host": {
        "workflowReferenceName": "5c5a4fe6-31d4-ef11-8eea-6045bd0804f0"
        // 这是Parse835Claims子流程的唯一ID
      },
      "body": {
        "text": "@outputs('Claim')",      // 输入参数1：理赔文本
        "text_1": "@variables('File GUID')" // 输入参数2：文件ID
      }
    },
    "runAfter": {
      "Claim": ["Succeeded"]  // 等待"Claim"步骤成功后再执行
    }
  }
}
```

### 子流程如何接收和返回数据

**子流程定义（Parse835Claims）：**

```json
{
  "triggers": {
    "manual": {
      "type": "Request",
      "inputs": {
        "schema": {
          "properties": {
            "text": {
              "title": "ClaimText",  // 接收理赔文本
              "type": "string"
            },
            "text_1": {
              "title": "FileGUID",   // 接收文件ID
              "type": "string"
            }
          }
        }
      }
    }
  },
  "actions": {
    // ... 处理逻辑 ...
    "Respond_to_a_Power_App_or_flow": {
      "type": "Response",
      "inputs": {
        "body": {
          "parsedclaim": "@{outputs('Parsed_Claim')}"  // 返回解析结果
        }
      }
    }
  }
}
```

---

## 🎯 本项目中的四个子流程详解

### 1. ParseHeader835 子流程

**作用：** 解析835文件的头部信息

**调用方式：**
```json
主流程 → ParseHeader835
输入：文件头文本
输出：JSON格式的文件头数据
```

**工作流程：**
```
接收文件头文本
   ↓
调用Azure OpenAI + Parse索引
   ↓
解析出付款人信息、日期等
   ↓
返回JSON数据给主流程
```

---

### 2. Parse835Claims 子流程

**作用：** 解析单个835理赔记录

**调用方式：**
```json
主流程 → Parse835Claims
输入：单个理赔的文本片段
输出：结构化的JSON理赔数据
```

**工作流程：**
```
接收理赔文本（CLP*开头的段落）
   ↓
调用Azure OpenAI + Parse索引
   ↓
解析出Claim ID、患者信息、费用、CARC代码等
   ↓
返回JSON数据给主流程
```

**关键点：**
- 主流程会循环调用这个子流程
- 一个835文件可能包含多个理赔
- 每个理赔都需要单独解析

---

### 3. Parse837Claims 子流程

**作用：** 解析837理赔申请文件

**调用方式：**
```json
主流程 → Parse837Claims
输入：837文件完整内容
输出：结构化的JSON理赔申请数据
```

**工作流程：**
```
接收837文件内容
   ↓
调用Azure OpenAI + Parse索引
   ↓
解析出理赔申请详情、诊断代码、程序代码等
   ↓
返回JSON数据给主流程
```

---

### 4. GetRec 子流程

**作用：** 生成理赔拒绝处理建议

**调用方式：**
```json
主流程 → GetRec
输入：CARC代码、RARC代码、保险公司名称、File GUID
输出：HTML格式的处理建议 + 引用来源
```

**工作流程：**
```
接收CARC/RARC代码和保险公司信息
   ↓
构建查询提示词
   ↓
调用Azure OpenAI + Recs索引（RAG）
   ↓
检索相关理赔指南文档
   ↓
生成个性化处理建议
   ↓
返回HTML建议和引用来源给主流程
```

**关键点：**
- 可以被主流程自动调用
- 也可以被用户从Power Apps手动触发
- 使用RAG技术检索理赔指南

---

## 🔄 完整调用流程示例

### 场景：处理一个835文件

```
1. 用户上传835文件到SharePoint
   ↓
2. 主流程触发（DenialNavigator_MainProcessFlow）
   ↓
3. 主流程读取文件内容
   ↓
4. 主流程调用子流程1：ParseHeader835
   ├─ 输入：文件头文本
   ├─ 处理：解析文件头
   └─ 输出：文件头JSON → 返回主流程
   ↓
5. 主流程分割文件，得到多个理赔
   ↓
6. 主流程循环处理每个理赔：
   │
   ├─ 调用子流程2：Parse835Claims
   │  ├─ 输入：单个理赔文本
   │  ├─ 处理：解析理赔数据
   │  └─ 输出：理赔JSON → 返回主流程
   │
   ├─ 主流程存储理赔数据到Dataverse
   │
   ├─ 主流程提取CARC代码
   │
   └─ 对每个CARC代码：
       │
       └─ 调用子流程4：GetRec
          ├─ 输入：CARC代码、RARC代码、保险公司
          ├─ 处理：生成处理建议
          └─ 输出：HTML建议 → 返回主流程
          │
          └─ 主流程存储推荐到Dataverse
   ↓
7. 主流程完成，所有数据已存储
```

---

## 💡 子流程 vs 普通步骤

### 普通步骤（Action）

```json
{
  "Compose_Text": {
    "type": "Compose",  // 普通步骤
    "inputs": "Hello World"
  }
}
```

**特点：**
- 在主流程内部执行
- 不能独立运行
- 不能接收外部调用

### 子流程（Child Workflow）

```json
{
  "Parse_835_Claims": {
    "type": "Workflow",  // 子流程
    "inputs": {
      "host": {
        "workflowReferenceName": "子流程ID"
      },
      "body": {
        "text": "输入参数"
      }
    }
  }
}
```

**特点：**
- 是一个独立的流程
- 可以被多个主流程调用
- 可以接收输入参数
- 可以返回输出结果
- 可以独立测试和运行

---

## 🎓 类比总结

### 编程类比

| 概念 | 编程 | Power Automate |
|------|------|----------------|
| 主程序 | `main()` 函数 | 主流程 |
| 函数 | `function parseClaim()` | 子流程 |
| 函数调用 | `parseClaim(text)` | 调用子流程 |
| 参数传递 | `function(text)` | `body: { text: "..." }` |
| 返回值 | `return result` | `Response` 操作 |

### 日常类比

**主流程 = 餐厅经理**
- 协调整个餐厅运营
- 决定什么时候做什么

**子流程 = 专业厨师**
- 负责特定菜品的制作
- 接收订单（输入）
- 完成制作（处理）
- 上菜（输出）

---

## ✅ 关键要点总结

1. **子流程是独立的流程**
   - 有自己的触发器和操作
   - 可以被多个主流程调用

2. **主流程调用子流程**
   - 传递输入参数
   - 等待子流程完成
   - 接收返回结果

3. **使用子流程的好处**
   - 代码复用
   - 模块化设计
   - 易于维护

4. **本项目有4个子流程**
   - ParseHeader835：解析文件头
   - Parse835Claims：解析835理赔
   - Parse837Claims：解析837理赔
   - GetRec：生成处理建议

5. **子流程可以独立运行**
   - 可以手动触发测试
   - 也可以被主流程自动调用

---

## 🔍 如何识别子流程？

### 在代码中识别

1. **类型标识**
   ```json
   "type": "Workflow"  // 这是子流程
   ```

2. **引用方式**
   ```json
   "workflowReferenceName": "唯一ID"
   ```

3. **文件命名**
   - 主流程：`DenialNavigator_MainProcessFlow`
   - 子流程：`DenialNavigator_Child_XXX`

### 在Power Automate界面中识别

- 主流程：有触发器（如"文件创建"）
- 子流程：触发器类型是"手动"或"请求"
- 子流程可以被其他流程调用

---

希望这个解释帮助您理解什么是工作子流程！如果还有疑问，请随时提问。
