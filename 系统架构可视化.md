# RHAIL Claims Denial Navigator 系统架构可视化

## 🏗️ 整体系统架构图

```mermaid
graph TB
    subgraph "用户层"
        User[👤 用户]
        PowerApp[📱 Power Apps用户界面]
    end
    
    subgraph "数据输入层"
        SharePoint[📁 SharePoint文件库]
    end
    
    subgraph "工作流层 - Power Automate"
        Trigger[⚡ 触发器监听文件创建]
        MainFlow[🔄 主流程MainProcessFlow]
        
        subgraph "子流程组"
            ParseHeader[📋 解析文件头ParseHeader835]
            Parse835[📄 解析835理赔Parse835Claims]
            Parse837[📄 解析837理赔Parse837Claims]
            GetRec[💡 获取推荐GetRec]
        end
    end
    
    subgraph "AI服务层"
        AzureOpenAI[🤖 Azure OpenAI GPT-4o]
        
        subgraph "RAG数据源"
            AzureSearch[🔍 Azure AI Search]
            ParseIndex[📚 Parse索引解析指南]
            RecsIndex[📚 Recs索引理赔指南]
        end
        
        BlobStorage[💾 Azure Blob Storage]
    end
    
    subgraph "数据存储层"
        Dataverse[(🗄️ Dataverse数据库)]
    end
    
    User -->|上传文件| SharePoint
    SharePoint -->|文件创建事件| Trigger
    Trigger --> MainFlow
    
    MainFlow --> ParseHeader
    MainFlow --> Parse835
    MainFlow --> Parse837
    MainFlow --> GetRec
    
    ParseHeader -->|HTTP POST| AzureOpenAI
    Parse835 -->|HTTP POST| AzureOpenAI
    Parse837 -->|HTTP POST| AzureOpenAI
    GetRec -->|HTTP POST| AzureOpenAI
    
    AzureOpenAI <-->|检索文档| AzureSearch
    AzureSearch --> ParseIndex
    AzureSearch --> RecsIndex
    BlobStorage -->|索引文档| AzureSearch
    
    AzureOpenAI -->|返回JSON| ParseHeader
    AzureOpenAI -->|返回JSON| Parse835
    AzureOpenAI -->|返回HTML建议| GetRec
    
    ParseHeader -->|存储数据| Dataverse
    Parse835 -->|存储数据| Dataverse
    Parse837 -->|存储数据| Dataverse
    GetRec -->|存储推荐| Dataverse
    
    Dataverse -->|显示结果| PowerApp
    PowerApp --> User
    
    style User fill:#e1f5ff
    style PowerApp fill:#e1f5ff
    style SharePoint fill:#fff4e1
    style Trigger fill:#ffe1e1
    style MainFlow fill:#ffe1e1
    style AzureOpenAI fill:#e1ffe1
    style AzureSearch fill:#e1ffe1
    style Dataverse fill:#f0e1ff
```

## 🔄 详细数据流程图

### 流程1: 文件解析流程

```mermaid
sequenceDiagram
    participant User as 👤 用户
    participant SP as 📁 SharePoint
    participant PA as ⚡ Power Automate
    participant ParseFlow as 📄 Parse835Claims子流程
    participant AOI as 🤖 Azure OpenAI
    participant AS as 🔍 Azure Search
    participant DV as 🗄️ Dataverse
    participant App as 📱 Power Apps
    
    User->>SP: 上传835/837文件
    SP->>PA: 触发文件创建事件
    PA->>PA: 读取文件内容并分割成多个理赔
    
    Note over PA: 对每个理赔循环处理
    
    PA->>ParseFlow: 调用子流程传入理赔文本
    ParseFlow->>AOI: HTTP POST请求解析理赔(data_sources: parse索引)
    AOI->>AS: 自动检索Parse索引查找解析指南
    AS-->>AOI: 返回相关文档
    AOI->>AOI: 使用RAG增强生成解析为JSON
    AOI-->>ParseFlow: 返回结构化JSON
    ParseFlow-->>PA: 返回解析结果
    
    PA->>DV: 存储理赔数据到ClaimResponses表
    
    Note over PA: 处理Service Lines提取CARC代码
    
    PA->>PA: 对每个CARC代码调用GetRec生成推荐
    PA->>DV: 存储推荐到ClaimAdjustmentCodes表
    
    DV-->>App: 数据更新
    App-->>User: 显示解析结果和推荐
```

### 流程2: 推荐生成流程

```mermaid
sequenceDiagram
    participant User as 👤 用户
    participant App as 📱 Power Apps
    participant MainFlow as 🔄 主流程
    participant GetRec as 💡 GetRec子流程
    participant AOI as 🤖 Azure OpenAI
    participant AS as 🔍 Azure Search
    participant DV as 🗄️ Dataverse
    
    Note over MainFlow: 场景1: 主流程自动调用
    
    MainFlow->>MainFlow: 解析理赔后提取CARC和RARC代码
    MainFlow->>GetRec: 调用GetRec子流程传入CARC/RARC/保险公司/FileGUID
    
    Note over App: 场景2: 用户手动触发
    
    User->>App: 选择理赔记录点击获取推荐按钮
    App->>GetRec: 调用GetRec流程传入CARC/RARC/保险公司/FileGUID
    
    Note over GetRec: 构建查询提示词
    
    GetRec->>AOI: HTTP POST请求生成推荐(data_sources: Recs索引)
    
    Note over AOI,AS: RAG检索过程
    AOI->>AS: 向量搜索查询相关文档
    AS->>AS: 检索理赔指南政策文档用户反馈
    AS-->>AOI: 返回Top 5相关文档
    
    AOI->>AOI: 基于检索文档生成个性化建议
    AOI-->>GetRec: 返回HTML格式建议和引用来源
    
    GetRec-->>MainFlow: 返回推荐结果(场景1)
    GetRec-->>App: 返回推荐结果(场景2)
    
    MainFlow->>DV: 存储推荐到ClaimAdjustmentCodes表
    DV-->>App: 数据更新
    App-->>User: 显示处理建议
```

## 🧩 组件交互图

```mermaid
graph LR
    subgraph "Power Platform生态"
        PA[Power Automate工作流引擎]
        DV[Dataverse数据库]
        App[Power Apps应用界面]
    end
    
    subgraph "Azure AI服务"
        AOI[Azure OpenAI GPT-4o]
        AS[Azure AI Search向量搜索]
    end
    
    subgraph "存储服务"
        SP[SharePoint文件库]
        BS[Blob Storage文档存储]
    end
    
    SP -.文件上传.-> PA
    PA -.HTTP请求.-> AOI
    AOI -.RAG检索.-> AS
    AS -.索引文档.-> BS
    AOI -.返回结果.-> PA
    PA -.存储数据.-> DV
    DV -.查询数据.-> App
    
    style PA fill:#ff6b6b
    style AOI fill:#4ecdc4
    style AS fill:#4ecdc4
    style DV fill:#95e1d3
    style App fill:#f38181
```

## 📊 RAG工作原理详解

```mermaid
graph TD
    subgraph "步骤1: 文档准备"
        Doc1[理赔处理指南.pdf]
        Doc2[保险公司政策.docx]
        Doc3[用户反馈.txt]
        Doc4[CARC代码定义.csv]
        
        Doc1 --> BS[上传到Blob Storage]
        Doc2 --> BS
        Doc3 --> BS
        Doc4 --> BS
    end
    
    subgraph "步骤2: 索引构建"
        BS --> Indexer[索引器运行]
        Indexer -->|提取文本| Parser[文档解析器]
        Parser -->|生成向量| Embedding[向量化]
        Embedding -->|存储| ASIndex[Azure Search索引filerecs-autocreate]
    end
    
    subgraph "步骤3: 查询时RAG"
        Query[用户查询:CARCCode CO-16 RARCCode N255]
        Query --> AOI[Azure OpenAI]
        AOI -->|自动触发| VectorSearch[向量搜索]
        VectorSearch -->|查询| ASIndex
        ASIndex -->|返回Top 5| RelevantDocs[相关文档片段]
        RelevantDocs --> AOI
        AOI -->|增强生成| Response[个性化建议和引用来源]
    end
    
    style BS fill:#ffe1e1
    style ASIndex fill:#e1ffe1
    style AOI fill:#e1f5ff
    style Response fill:#fff4e1
```

## 🔀 主流程决策树

```mermaid
flowchart TD
    Start([文件上传到SharePoint]) --> CheckType{检查文件类型}
    
    CheckType -->|835文件| Process835[处理835文件]
    CheckType -->|837文件| Process837[处理837文件]
    
    Process835 --> ParseHeader[解析文件头ParseHeader835]
    ParseHeader --> ParseClaims[解析理赔数据Parse835Claims]
    
    Process837 --> Parse837Claims[解析837理赔Parse837Claims]
    
    ParseClaims --> StoreData[存储到Dataverse]
    Parse837Claims --> StoreData
    
    StoreData --> ProcessServiceLines[处理Service Lines提取CARC代码]
    
    ProcessServiceLines --> HasCARC{是否有CARC代码?}
    
    HasCARC -->|是| GetRecommendation[对每个CARC调用GetRec生成推荐]
    HasCARC -->|否| Complete[完成]
    
    GetRecommendation --> StoreRec[存储推荐到ClaimAdjustmentCodes表]
    StoreRec --> Complete
    
    Complete --> Display([在Power Apps显示])
    
    style Start fill:#e1f5ff
    style CheckType fill:#fff4e1
    style Process835 fill:#ffe1e1
    style Process837 fill:#ffe1e1
    style GetRecommendation fill:#e1ffe1
    style Display fill:#f0e1ff
```

## 🗂️ 数据流架构

```mermaid
graph TB
    subgraph "输入数据"
        Input1[835文件文本格式]
        Input2[837文件文本格式]
    end
    
    subgraph "处理层"
        P1[Power Automate解析流程]
        P2[Azure OpenAI AI解析]
        P3[Azure Search RAG检索]
    end
    
    subgraph "输出数据"
        O1[结构化JSON理赔数据]
        O2[HTML建议处理方案]
        O3[引用来源Citations]
    end
    
    subgraph "存储"
        S1[(Dataverse表1 Claims)]
        S2[(Dataverse表2 ClaimResponses)]
        S3[(Dataverse表3 Recommendations)]
    end
    
    Input1 --> P1
    Input2 --> P1
    P1 --> P2
    P2 <--> P3
    P2 --> O1
    P2 --> O2
    P2 --> O3
    
    O1 --> S1
    O1 --> S2
    O2 --> S3
    O3 --> S3
    
    style Input1 fill:#ffe1e1
    style Input2 fill:#ffe1e1
    style P2 fill:#e1ffe1
    style P3 fill:#e1ffe1
    style O2 fill:#fff4e1
    style S1 fill:#f0e1ff
    style S2 fill:#f0e1ff
    style S3 fill:#f0e1ff
```

## 🔐 安全与访问控制

```mermaid
graph LR
    subgraph "用户认证"
        User[用户] --> Auth[Azure AD认证]
    end
    
    subgraph "Power Platform安全"
        Auth --> PP[Power Platform权限控制]
        PP --> PA[Power Automate连接引用]
        PP --> App[Power Apps角色权限]
    end
    
    subgraph "Azure资源安全"
        PA --> APIKey1[API密钥环境变量]
        APIKey1 --> AOI[Azure OpenAI私有端点]
        APIKey1 --> AS[Azure Search API密钥]
    end
    
    subgraph "数据安全"
        AOI --> VNet[虚拟网络私有连接]
        AS --> VNet
        VNet --> PrivateEndpoint[私有端点]
    end
    
    style Auth fill:#ffe1e1
    style PP fill:#e1ffe1
    style VNet fill:#e1f5ff
    style PrivateEndpoint fill:#fff4e1
```

## 📈 性能优化流程

```mermaid
graph TD
    subgraph "并发控制"
        Trigger[文件上传触发器]
        Trigger --> Queue[队列管理最大25个等待]
        Queue --> Process[串行处理避免冲突]
    end
    
    subgraph "缓存策略"
        Process --> CheckCache{检查缓存}
        CheckCache -->|命中| UseCache[使用缓存结果]
        CheckCache -->|未命中| CallAPI[调用API]
    end
    
    subgraph "API优化"
        CallAPI --> Batch[批量处理]
        Batch --> Retry[重试机制]
        Retry --> Timeout[超时控制]
    end
    
    subgraph "索引优化"
        AS[Azure Search]
        AS --> VectorIndex[向量索引]
        AS --> KeywordIndex[关键词索引]
        VectorIndex --> TopN[Top 5检索]
    end
    
    style Queue fill:#ffe1e1
    style CheckCache fill:#e1ffe1
    style Batch fill:#e1f5ff
    style VectorIndex fill:#fff4e1
```

## 🎯 关键区别对比

```mermaid
graph TB
    subgraph "传统LLM架构"
        T1[用户输入] --> T2[MCP服务器]
        T2 --> T3[工具调用]
        T3 --> T4[LLM处理]
        T4 --> T5[输出结果]
    end
    
    subgraph "本项目RAG架构"
        P1[文件上传] --> P2[Power Automate]
        P2 --> P3[Azure OpenAI]
        P3 --> P4[自动RAG检索]
        P4 --> P5[Azure Search]
        P5 --> P6[文档增强]
        P6 --> P7[生成结果]
        P7 --> P8[存储到Dataverse]
    end
    
    style T2 fill:#ffcccc
    style P2 fill:#ccffcc
    style P4 fill:#cceeff
    style P5 fill:#cceeff
```

---

## 📝 图例说明

- 🔵 **蓝色**: 用户界面和交互
- 🟡 **黄色**: 数据输入和存储
- 🔴 **红色**: 工作流和处理逻辑
- 🟢 **绿色**: AI服务和RAG
- 🟣 **紫色**: 数据库和持久化

## 💡 关键要点

1. **不是MCP架构**: 使用Power Automate + RAG模式
2. **自动化流程**: 从文件上传到结果展示全自动
3. **RAG增强**: Azure Search提供上下文文档
4. **低代码平台**: 基于Power Platform，易于维护
5. **企业级集成**: 与Microsoft生态系统深度集成
