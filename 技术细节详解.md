# RHAIL Claims Denial Navigator 技术细节详解

## 📋 目录
1. [数据输入层 - SharePoint文件库](#1-数据输入层---sharepoint文件库)
2. [主处理流程](#2-主处理流程)
3. [三个解析子流程](#3-三个解析子流程)
4. [GetRec是什么](#4-getrec是什么)
5. [GetRec如何工作](#5-getrec如何工作)
6. [数据存储结构](#6-数据存储结构)
7. [完整数据流程图](#7-完整数据流程图)

---

## 1. 数据输入层 - SharePoint文件库

### 📁 输入的文件类型

**用户上传到SharePoint的文件：**

1. **835文件** (Electronic Remittance Advice - ERA)
   - **格式**: 文本文件（.txt或.835）
   - **内容**: 保险公司返回的理赔处理结果
   - **包含信息**:
     - 理赔ID (Claim ID)
     - 患者信息 (Patient ID, Name)
     - 支付金额 (Payment Amount)
     - 拒绝代码 (CARC - Claim Adjustment Reason Code)
     - 备注代码 (RARC - Remittance Advice Remark Code)
     - 服务行信息 (Service Lines)

2. **837文件** (Electronic Health Care Claim)
   - **格式**: 文本文件（.txt或.837）
   - **内容**: 医疗机构提交的原始理赔申请
   - **包含信息**:
     - 理赔申请详情
     - 诊断代码 (Diagnosis Codes)
     - 程序代码 (Procedure Codes)
     - 服务日期
     - 费用信息

### 🔍 文件格式示例

**835文件片段示例：**
```
ISA*00*          *00*          *ZZ*123456789      *ZZ*987654321      *240101*1200*^*00501*000000001*0*P*:~
GS*HP*123456789*987654321*20240101*1200*1*X*005010X221A1~
ST*835*0001*005010X221A1~
BPR*I*1500.00*C*CHK*123456789*DA*123456789*20240101*123456789*01*987654321*DA*987654321~
...
CLP*123456789*1*1500.00*100.00*0.00*12*123456789~
CAS*CO*45*100.00~
...
```

**关键点：**
- 835文件是**保险公司返回的**，告诉医院哪些理赔被拒绝/调整
- 837文件是**医院提交的**原始理赔申请
- 系统主要处理835文件，837文件用于匹配和对比

---

## 2. 主处理流程

### 🔄 DenialNavigator_MainProcessFlow 工作流程

**触发条件：**
- 用户上传文件到SharePoint指定库
- Power Automate每1分钟检查一次新文件
- 检测到新文件后自动触发主流程

**主流程步骤：**

```
1. 检测文件类型
   ├─ 835文件 → 处理835流程
   └─ 837文件 → 处理837流程

2. 读取文件内容
   └─ 从SharePoint获取文件文本

3. 解析文件头 (仅835文件)
   └─ 调用 ParseHeader835 子流程

4. 分割理赔数据
   └─ 835文件可能包含多个理赔，按"CLP*"分割

5. 循环处理每个理赔
   ├─ 调用 Parse835Claims 解析单个理赔
   ├─ 存储理赔数据到Dataverse
   ├─ 处理Service Lines
   ├─ 提取CARC代码
   └─ 对每个CARC代码调用GetRec生成推荐

6. 存储结果
   └─ 所有数据存储到Dataverse数据库
```

---

## 3. 三个解析子流程

### 📋 解析流程详解

#### 3.1 ParseHeader835 - 解析835文件头

**作用：** 解析835文件的头部信息

**输入：**
- 835文件头部分（从ST*到第一个N1*PE之间的内容）

**处理：**
- 调用Azure OpenAI + Parse索引
- 提取：
  - 付款人信息 (Payer Information)
  - 文件创建日期
  - 交易集信息

**输出：**
- JSON格式的文件头数据
- 存储到Dataverse的ProcessedFile表

**代码位置：** `DenialNavigator_Child_ParseHeader835`

---

#### 3.2 Parse835Claims - 解析835理赔数据

**作用：** 解析835文件中的每个理赔记录

**输入：**
- 单个理赔的文本片段（CLP*开头的段落）

**处理：**
- 调用Azure OpenAI + Parse索引
- 提取：
  - Claim ID (理赔ID)
  - Claim ICN (理赔控制号)
  - Patient ID, Name (患者信息)
  - Total Cost, Total Paid (费用信息)
  - RARC Codes (备注代码数组)
  - Service Lines (服务行数组，包含CARC代码)

**输出：**
- 结构化的JSON数据
- 示例格式：
```json
{
  "claim": {
    "claimId": "123456789",
    "claimICN": "987654321",
    "patientId": "55555555555",
    "patientFirstName": "Bob",
    "patientLastName": "Thomas",
    "totalCost": 3002.00,
    "totalPaid": 0.00,
    "rarc": ["MA48", "N706"],
    "serviceLines": [
      {
        "serviceCode": "HC:97110:GP",
        "chargeAmount": 115.00,
        "paidAmount": 0,
        "cas": [
          {
            "groupCode": "PI",
            "carc": "226",
            "amount": 115.00
          }
        ]
      }
    ]
  }
}
```

**代码位置：** `DenialNavigator_Child_Parse835Claims`

---

#### 3.3 Parse837Claims - 解析837理赔数据

**作用：** 解析837文件中的理赔申请信息

**输入：**
- 837文件的完整内容

**处理：**
- 调用Azure OpenAI + Parse索引
- 提取：
  - 理赔申请详情
  - 诊断代码及描述
  - 程序代码及描述
  - 服务日期
  - 总费用

**输出：**
- 结构化的JSON数据
- 存储到Dataverse的Claims表

**代码位置：** `DenialNavigator_Child_Parse837Claims`

---

### 🔑 解析的关键技术

**所有解析都使用相同的技术栈：**

1. **Azure OpenAI GPT-4o模型**
   - 接收原始文本
   - 使用AI理解文本结构
   - 生成结构化JSON

2. **Azure AI Search - Parse索引**
   - 存储解析指南和示例
   - 通过RAG增强AI的解析能力
   - 提供解析模板和规则

3. **RAG工作原理：**
   ```
   用户查询(835文本) 
   → Azure OpenAI 
   → 自动检索Parse索引 
   → 找到解析示例和指南 
   → 使用这些文档增强生成 
   → 返回结构化JSON
   ```

---

## 4. GetRec是什么

### 💡 GetRec = Get Recommendation（获取推荐）

**GetRec不是数据库，而是一个Power Automate子流程！**

**全名：** `DenialNavigator_Child_GetRec`

**作用：** 根据理赔拒绝代码（CARC和RARC）生成处理建议

**触发方式：**

1. **自动触发**（主流程中）
   - 解析完理赔后，发现CARC代码
   - 主流程自动调用GetRec
   - 为每个CARC代码生成推荐

2. **手动触发**（用户操作）
   - 用户在Power Apps中选择理赔记录
   - 点击"获取推荐"按钮
   - 手动调用GetRec流程

**输入参数：**
- `text`: CARC代码（如 "CO-16"）
- `text_1`: RARC代码（如 "N255"）
- `text_2`: 保险公司名称（如 "North Dakota Medicaid"）
- `text_3`: File GUID（文件ID）

---

## 5. GetRec如何工作

### 🔄 GetRec详细工作流程

```
步骤1: 构建查询提示词
   ↓
步骤2: 调用Azure OpenAI
   ├─ 传入CARC/RARC/保险公司信息
   └─ 配置data_sources: Recs索引
   ↓
步骤3: Azure OpenAI自动RAG检索
   ├─ 查询Azure AI Search的Recs索引
   ├─ 使用向量搜索找到Top 5相关文档
   └─ 文档可能包括：
      - 理赔处理指南
      - 保险公司政策文档
      - CARC/RARC代码定义
      - 用户反馈文档
   ↓
步骤4: 生成推荐
   ├─ 基于检索到的文档
   ├─ 生成HTML格式的处理建议
   └─ 包含引用来源(citations)
   ↓
步骤5: 返回结果
   └─ HTML建议 + 引用来源
```

### 📊 GetRec使用的技术

#### 5.1 Azure AI Search - Recs索引

**索引名称：** `filerecs-autocreate`

**存储内容：**
- 理赔处理指南（PDF/DOCX）
- 保险公司政策文档
- CARC代码定义和说明
- RARC代码定义和说明
- 用户反馈文档（`SubmittedUserFeedback.txt`）

**索引构建过程：**
```
文档上传到Blob Storage (recs容器)
   ↓
索引器自动运行
   ↓
提取文本内容
   ↓
生成向量嵌入(Vector Embedding)
   ↓
存储到Azure Search索引
```

#### 5.2 RAG检索过程

**当GetRec调用Azure OpenAI时：**

```json
{
  "messages": [
    {
      "role": "system",
      "content": "You are an AI assistant that helps people fix medical billing files..."
    },
    {
      "role": "user",
      "content": "Provide recommendations on how to correct the claim for [保险公司]. CARC: [代码]. RARC: [代码]"
    }
  ],
  "data_sources": [
    {
      "type": "azure_search",
      "parameters": {
        "endpoint": "Azure Search URL",
        "index_name": "filerecs-autocreate",  // 使用Recs索引
        "authentication": {
          "type": "api_key",
          "key": "API Key"
        },
        "strictness": 5,
        "top_n_documents": 5,  // 检索前5个相关文档
        "fields_mapping": {
          "content_fields": ["content", "metadata_storage_name"],
          "vector_fields": ["contentVector"]  // 向量搜索字段
        }
      }
    }
  ]
}
```

**Azure OpenAI内部处理：**
1. 接收用户查询（CARC/RARC代码）
2. **自动触发**向量搜索
3. 在Recs索引中查找相关文档
4. 返回Top 5最相关的文档片段
5. 将这些文档作为上下文
6. 生成个性化的处理建议

#### 5.3 输出格式

**GetRec返回HTML格式的建议：**

```html
<div class="ck-content">
  <h2>CARC: CO-16</h2>
  <p><strong>Claim/service lacks information or has submission/billing error(s).</strong></p>
  
  <h3>RARC:</h3>
  <p>N255: Missing/incomplete/invalid billing provider taxonomy.</p>
  
  <h3>Recommendations and Steps:</h3>
  <p><strong>Identify Missing Information:</strong></p>
  <p>Review the claim to determine what specific information is missing...</p>
  
  <p><strong>Gather Required Information:</strong></p>
  <p>Ensure you have the correct billing provider taxonomy...</p>
  
  <h3>User Feedback:</h3>
  <p><strong>No matching user feedback from SubmittedUserFeedback.txt for CARC CO-16</strong></p>
</div>
```

**同时返回引用来源：**
```json
{
  "citations": [
    {
      "content": "文档片段内容",
      "title": "文档名称",
      "filepath": "文档路径",
      "chunk_id": "片段ID"
    }
  ]
}
```

---

## 6. 数据存储结构

### 🗄️ Dataverse数据库表

#### 6.1 ProcessedFile表 (cr1a1_ProcessedFile)
**存储：** 上传的文件元数据
- 文件ID
- 文件名
- 上传时间
- 处理状态
- 文件类型（835/837）

#### 6.2 ClaimResponses表 (cp835_claimresponses)
**存储：** 解析后的835理赔数据
- Claim ID
- Claim ICN
- Patient ID, Name
- Total Cost, Total Paid
- 理赔日期
- JSON响应数据
- 关联的File ID

#### 6.3 Claims表 (rhail_claims)
**存储：** 837理赔申请数据
- Claim ID
- Patient信息
- Service Date
- Total Claim Charges
- JSON数据

#### 6.4 ServiceLines表 (cp835_servicelines)
**存储：** 服务行信息
- Service Code
- Charge Amount
- Paid Amount
- 关联的Claim Response

#### 6.5 ClaimAdjustmentCodes表 (cp835_claimadjustmentcodes)
**存储：** **GetRec生成的推荐**
- CARC代码
- 推荐内容（HTML格式）
- 推荐文本（纯文本）
- 引用来源
- 关联的Service Line
- 关联的Claim Response

#### 6.6 ReasonCodes表 (cp835_reasoncodes)
**存储：** RARC代码信息
- RARC代码
- 定义
- 关联的Claim Response

---

## 7. 完整数据流程图

### 📊 完整流程可视化

```
┌─────────────────────────────────────────────────────────────┐
│ 步骤1: 文件上传                                              │
└─────────────────────────────────────────────────────────────┘
         │
         │ 用户上传835/837文件到SharePoint
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤2: 主流程触发                                            │
│ DenialNavigator_MainProcessFlow                             │
└─────────────────────────────────────────────────────────────┘
         │
         │ 检测文件类型
         ↓
    ┌────┴────┐
    │ 835文件 │  │ 837文件 │
    └────┬────┘  └────┬────┘
         │            │
         ↓            ↓
┌────────────────┐ ┌──────────────────┐
│ ParseHeader835 │ │ Parse837Claims    │
│ 解析文件头     │ │ 解析837理赔       │
└────────┬───────┘ └────────┬──────────┘
         │                  │
         ↓                  │
┌────────────────┐          │
│ Parse835Claims │          │
│ 解析835理赔    │          │
└────────┬───────┘          │
         │                  │
         └────────┬─────────┘
                  │
                  ↓
         ┌────────────────┐
         │ 存储到Dataverse│
         │ - ClaimResponses│
         │ - ServiceLines  │
         └────────┬────────┘
                  │
                  ↓
         ┌────────────────┐
         │ 提取CARC代码   │
         │ 从Service Lines│
         └────────┬────────┘
                  │
                  ↓
         ┌────────────────┐
         │ 对每个CARC代码 │
         │ 调用GetRec     │
         └────────┬────────┘
                  │
                  ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤3: GetRec生成推荐                                       │
│ DenialNavigator_Child_GetRec                                │
└─────────────────────────────────────────────────────────────┘
         │
         │ 构建查询: CARC + RARC + 保险公司
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤4: Azure OpenAI + RAG                                  │
└─────────────────────────────────────────────────────────────┘
         │
         │ HTTP POST to Azure OpenAI
         │ data_sources: Recs索引
         ↓
┌─────────────────────────────────────────────────────────────┐
│ Azure OpenAI自动处理:                                       │
│ 1. 接收查询                                                 │
│ 2. 自动查询Azure Search Recs索引                           │
│ 3. 向量搜索找到Top 5相关文档                              │
│ 4. 使用文档作为上下文                                       │
│ 5. 生成个性化建议                                           │
└─────────────────────────────────────────────────────────────┘
         │
         │ 返回HTML建议 + 引用来源
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤5: 存储推荐                                             │
│ 存储到ClaimAdjustmentCodes表                               │
└─────────────────────────────────────────────────────────────┘
         │
         │ 数据更新
         ↓
┌─────────────────────────────────────────────────────────────┐
│ 步骤6: Power Apps显示                                      │
│ 用户查看解析结果和推荐建议                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 关键概念澄清

### ❌ 常见误解

1. **误解：** "解析结果会输入到GetRec"
   - **正确：** GetRec是独立调用的，输入是CARC/RARC代码，不是解析结果

2. **误解：** "GetRec是一个数据库"
   - **正确：** GetRec是一个Power Automate工作流子流程

3. **误解：** "GetRec会重新解析文件"
   - **正确：** GetRec不解析文件，它生成理赔拒绝处理建议

4. **误解：** "三个解析的结果都输入到GetRec"
   - **正确：** 只有解析出的CARC代码会触发GetRec，用于生成该代码的处理建议

### ✅ 正确理解

1. **文件输入：** 835和837文本文件上传到SharePoint

2. **解析流程：** 
   - ParseHeader835 → 解析文件头
   - Parse835Claims → 解析理赔数据
   - Parse837Claims → 解析837理赔

3. **数据存储：** 解析结果存储到Dataverse的各个表

4. **推荐生成：** 
   - 从解析结果中提取CARC代码
   - 调用GetRec子流程
   - GetRec使用Azure AI Search的Recs索引检索相关文档
   - 生成处理建议并存储

5. **RAG技术：** 
   - Azure OpenAI自动检索Azure Search索引
   - 使用向量搜索找到最相关文档
   - 基于文档生成个性化建议

---

## 📚 技术术语解释

### CARC (Claim Adjustment Reason Code)
- **定义：** 理赔调整原因代码
- **作用：** 说明为什么理赔被拒绝或调整
- **示例：** CO-16 = "Claim/service lacks information"

### RARC (Remittance Advice Remark Code)
- **定义：** 汇款通知备注代码
- **作用：** 提供额外的处理说明
- **示例：** N255 = "Missing/incomplete/invalid billing provider taxonomy"

### RAG (Retrieval-Augmented Generation)
- **定义：** 检索增强生成
- **工作原理：** 
  1. 检索相关文档
  2. 将文档作为上下文
  3. 基于上下文生成答案
- **优势：** 无需重新训练模型即可更新知识库

### Azure AI Search
- **作用：** 向量搜索引擎
- **两个索引：**
  - Parse索引：用于解析任务
  - Recs索引：用于推荐任务
- **功能：** 语义搜索、向量搜索、关键词搜索

---

## 💡 总结

1. **输入：** 835/837文本文件上传到SharePoint
2. **解析：** 三个子流程分别解析文件头、835理赔、837理赔
3. **存储：** 解析结果存储到Dataverse数据库
4. **推荐：** GetRec子流程根据CARC代码生成处理建议
5. **技术：** 使用Azure OpenAI + Azure AI Search的RAG技术
6. **输出：** HTML格式的建议和引用来源，存储在ClaimAdjustmentCodes表
